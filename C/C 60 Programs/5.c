/*5. Write a program to perform multiplication of two numbers. */
#include <stdio.h>
int main()
{
    int a, b, multiplication;
    printf("Enter the first Number :  \n");
    scanf("%d", &a);
    printf("Enter the second Number :  \n");
    scanf("%d", &b);
    printf("The multiplication of %d and %d is %d", a, b, multiplication = a * b);
    return 0;
}