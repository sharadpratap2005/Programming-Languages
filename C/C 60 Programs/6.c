/*6. Write a program to perform division of 2 numbers */

#include <stdio.h>
int main()
{

    int a, b;
    float division;
    printf("Enter the first Number : \n");
    scanf("%d", &a);
    printf("Enter the second Number : \n");
    scanf("%d", &b);
    printf("Division of %d and %d is %.2f", a, b, division = (float)a / b);
    return 0;
}