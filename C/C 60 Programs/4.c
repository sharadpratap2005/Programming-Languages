/*4. Write a program to perform sum of two numbers.*/

#include <stdio.h>
int main()
{
    int a, b, sum;
    printf("Enter the first Number :  \n");
    scanf("%d", &a);
    printf("Enter the second Number :  \n");
    scanf("%d", &b);
    printf("The sum of %d and %d is %d", a, b, sum = a + b);
    return 0;
}